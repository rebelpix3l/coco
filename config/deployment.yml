apiVersion: apps/v1
kind: Deployment
metadata:
  name: static
spec:
  replicas: 0
  selector:
    matchLabels:
      app: static
  template:
    metadata:
      labels:
        app: static
    spec:
      containers:
        - name: static
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080
        - name: static
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080


