apiVersion: apps/v1
kind: Deployment
metadata:
  name: static1
spec:
  replicas: 0
  selector:
    matchLabels:
      app: static2
  template:
    metadata:
      labels:
        app: static1
    spec:
      containers:
        - name: static4
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080
        - name: static5
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080


