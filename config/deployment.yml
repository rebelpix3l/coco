apiVersion: apps/v1
kind: Deployment
metadata:
  name: staticExample
spec:
  replicas: 0
  selector:
    matchLabels:
      app: staticExample
  template:
    metadata:
      labels:
        app: staticExample
    spec:
      containers:
        - name: staticExample
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080
        - name: staticExample
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080


