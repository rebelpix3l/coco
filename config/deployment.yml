apiVersion: apps/v1
kind: Deployment
metadata:
  name: static1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: static1  # Change to match the template labels
  template:
    metadata:
      labels:
        app: static1
    spec:
      containers:
        - name: static4
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080
        - name: static5
          image: <IMAGE>  # This placeholder will be replaced during the workflow
          ports:
            - containerPort: 8080
